

20260622 - error with s3 passthrough

while accessing the web service locally at http://127.0.0.1:5000/provenance_node/92
I get the following error trying to retrieve a node file from s3
unable to fetch s3://dig-gene-set-data/GTEx/genesets/adipose_tissue/models/HZ1/extractor/age70_20/genesets.gmt: HTTP 403
i am logged in through the aws cli



20260608 - 
add a /gene_set_graph GET call which takes in a gene_set_id and 
call a new method in db_utils that queries the database to return the gene set graph information 
from the provenance_node and provenance_edge tables
return a json list with the results from the following sql query:
select pedge.gene_set_id, pedge.provenance_edge_id as edge_id, pedge.label as edge_name,
pedge.source_node_id as source_id, snode.node_type as source_type, snode.name as source_name,
pedge.target_node_id as target_id, tnode.node_type as target_type, tnode.name as target_name
from provenance_node snode, provenance_node tnode, provenance_edge pedge
where pedge.gene_set_id = <gene_set_id>
and snode.provenance_node_id = pedge.source_node_id
and tnode.provenance_node_id = pedge.target_node_id



20260602 - adding web call for s3 data
add a file python-flask-server/utils/web_utils.py that has a method
that returns what is in the provenance_node.dcc_url database value for the given provenance_node_id
if the value is an s3 link, fetch the file at the s3 link
if the value is an http link, return the page at the link
if neither of these, return <invalid operation>
handle any errors from missing files, permissions or other issues with a page with the appropriate message
hook this meethod up to the app.py with GET web signature /provenance_node/<id>
make this GET method so that a web link to  /provenance_node/<id> will display or download the file at the link





20260531 - add provenance graph
add a /gene_set_provenance call, which calls a db_utils method that returns the gene set provenance graph 
from the following sqlite db tables:
gene_set
provennance
provenance_node
provenance_edge
return the results into the a json knowledge graph object of the format of the file 
/Users/mduby/Code/MiscWorkspace/JasonPortalTestCFDE20260331/data/provenance/HuBMAP_Liver__scRNA__Leiden5_FCN3.overlay.json



20250520 - create web service
in the python-flask-server directory, build a flask server that runs on python 3.12 
the server will return data from a sqlite database which is stored in the data/genseco_v0.sqlite file
create a file utils/db_utils.py that has a method to return all gene set data linked in the 
gene_set, provenance, gene_set_gene_symbol, gene_symbol tables by the gene_set.gene_set_id primary key
as a json object
link the db_utils method to the flask server returning the json data from the gene_set_id GET input parameter
export a pip requirements file to notes/requirements.txt




